model:

  encoder:
    module: 'LSTM'
    bidirection: True
    dim: [1024,1024,1024,1024]
    dropout: [0,0,0,0]

  attention:
    mode: 'loc'
    dim: 300

  decoder:
    module: 'LSTM'
    dim: 1024
    layer: 1
    dropout: 0
